<% if resource.errors.any? %>
  <script>
    <% resource.errors.full_messages.each do |message| %>
    new Noty ({
        type: 'erro',
        layout: 'topRight',
        timeout: '3000',
        theme: 'relax',
        text: '<%= message %>'
      }).show();
    <%end%>
  </script>
<% end %>
